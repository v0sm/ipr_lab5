apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: lab5-fastapi
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000